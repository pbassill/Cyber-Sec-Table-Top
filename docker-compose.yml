version: '3.8'

services:
  cyberquest:
    build: .
    ports:
      - "8080:80"
    volumes:
      - cyberquest-data:/var/www/html/data
    restart: unless-stopped
    environment:
      - PHP_MEMORY_LIMIT=128M

volumes:
  cyberquest-data:
    driver: local
